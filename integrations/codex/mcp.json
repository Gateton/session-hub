{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "hub": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/scripts/mcp-shim.mjs"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
